-- local Model = require("lapis.db.model").Model

-- local Field = Model:extend("data-users")

-- local field = {
--   criar = function (data)
--     local f = Field:create({
--       id_data               = data. ,
--       registration_user     = data.dado,
--     })
--     if u then
-- 			return true
-- 		end
-- 		return false
--   end ,
-- }