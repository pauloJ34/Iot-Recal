services:
  app:
    container_name: back-front
    image: lapis-modificada 
    restart: always
    working_dir: /app
    ports:
      - "8080:8080"
    volumes:
      - .:/app/

    command: "lapis server"